% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_sensitivities.R
\name{derive_sensitivities}
\alias{derive_sensitivities}
\title{Derive trait-varying sensitivities (αᵢ, βᵢ) and abiotic slope (θ, Γᵢ)}
\usage{
derive_sensitivities(fit_coeffs, Q_inv, inv_ids, lrt = TRUE)
}
\arguments{
\item{fit_coeffs}{A fitted \code{glmmTMB} model on \verb{(r_z + C_z + S_z) × (tr1 + tr2)}.}

\item{Q_inv}{Data frame with invader trait coordinates \code{tr1}, \code{tr2}
(rownames = invader IDs).}

\item{inv_ids}{Character vector of invader IDs specifying the output order.}

\item{lrt}{Logical \strong{or} character. If logical:
\itemize{
\item \code{TRUE} → Wald test; \code{FALSE} → no test.
If character, one of \code{"wald"}, \code{"lrt"}, or \code{"none"}. Default is equivalent
to \code{"wald"} for \code{TRUE} and \code{"none"} for \code{FALSE}.
}}
}
\value{
A list with components:
\describe{
\item{alpha_i, beta_i}{Nonnegative penalties for \code{C_z} and \code{S_z}.}
\item{alpha_signed_i, beta_signed_i}{Signed (pre-clamp) slopes.}
\item{theta0}{Intercept for \code{r_z} slope system.}
\item{theta_i}{Trait-varying \code{r_z} slope evaluated at invader traits.}
\item{gamma_i}{Either \code{theta_i} (if test is significant) or a constant
vector equal to \code{theta0} (if not).}
\item{a0,a1,a2; b0,b1,b2}{Coefficient components for \code{C_z} and \code{S_z}
systems: main effect and interactions with \code{tr1},\code{tr2}.}
\item{df}{Per-invader tidy data frame with traits, signed slopes,
clamping flags, and \code{theta_i}/\code{gamma_i}.}
\item{clamp_summary}{Counts and proportions of clamped \eqn{\alpha} and \eqn{\beta}.}
\item{use_theta}{Logical; \code{TRUE} if the chosen test was significant.}
\item{lrt}{\strong{Back-compat alias} of \code{test_tab} (data frame with test result).}
\item{test_tab}{Data frame with test statistic, df, and p-value (or \code{NULL}).}
\item{test_type}{One of \code{"wald"}, \code{"lrt"}, or \code{"none"}.}
\item{prior_note}{Reminder of the nonnegative-penalty prior.}
}
}
\description{
From an auxiliary GLMM on standardized predictors, extract fixed-effect
systems for \code{r_z}, \code{C_z}, and \code{S_z}, evaluate them at invader trait
coordinates, and (optionally) test whether the abiotic slope on \code{r_z}
should vary with traits (\code{tr1}, \code{tr2}). Biotic terms are enforced as
nonnegative penalties on invasion fitness.
}
\details{
\strong{Biotic penalties (prior/constraint).} Invasion fitness is modeled as
\deqn{\lambda_{is} = \Gamma_{is} \, r^{(z)}_{is} \;-\; \alpha_i \, C^{(z)}_{is} \;-\; \beta_i \, S^{(z)}_{is}.}
We therefore set \eqn{\alpha_i = \max(0, -\text{slope}_{C,i})} and
\eqn{\beta_i = \max(0, -\text{slope}_{S,i})}, where \eqn{\text{slope}_{C,i}}
and \eqn{\text{slope}_{S,i}} are the fitted (possibly trait-varying) slopes
for \code{C_z} and \code{S_z}. Signed versions (before clamping) are returned for
diagnostics.

\strong{Testing trait-varying abiotic slope.} The argument \code{lrt} controls
whether and how to test the joint null \eqn{H_0: r_z:tr1 = r_z:tr2 = 0}.
\itemize{
\item For backward compatibility, \code{lrt = TRUE} performs a \strong{Wald} joint χ²
test using the fixed-effects variance–covariance (no refit).
\item \code{lrt = FALSE} performs no test.
\item Alternatively, pass a character string: \code{"wald"}, \code{"lrt"}, or \code{"none"}.
\itemize{
\item \code{"lrt"} refits a reduced model (dropping both interactions) and runs a
likelihood-ratio test (\code{anova(..., test = "Chisq")}).
}
}

If the chosen test is significant at \eqn{\alpha = 0.05}, the function
uses the trait-varying \code{theta_i}; otherwise a constant \code{theta0} is used
for \code{gamma_i}.
}
\seealso{
\code{\link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB()}}, \code{\link[stats:anova]{stats::anova()}}
}
