% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardise_model_inputs.R
\name{standardise_model_inputs}
\alias{standardise_model_inputs}
\title{Standardise model inputs (no leakage) for residents and invaders}
\usage{
standardise_model_inputs(
  env_df = NULL,
  traits_res,
  traits_inv = NULL,
  drop_extra_invader_cols = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{env_df}{Optional \code{data.frame} (sites × environment). Numeric columns
are z-scored; non-numeric are preserved.}

\item{traits_res}{\code{data.frame} (residents × traits). Mixed types allowed;
numeric columns are z-scored.}

\item{traits_inv}{Optional \code{data.frame} (invaders × traits). Must contain at
least the trait columns present in \code{traits_res}. Numeric columns are scaled
using \strong{resident} means/SDs; factors are coerced to resident levels.}

\item{drop_extra_invader_cols}{Logical; if \code{TRUE}, invader-only columns are
dropped (not used downstream). If \code{FALSE}, they are still dropped for
alignment but flagged in \code{info$notes}.}

\item{verbose}{Logical; print messages about what was standardised/coerced.}
}
\value{
A named \code{list} with components:
\describe{
\item{env_df_z}{Environment table with numeric columns z-scored (or \code{NULL}).}
\item{traits_res_glmm}{Resident trait table with numeric columns z-scored.}
\item{traits_inv_glmm}{Invader trait table scaled to resident moments and factor levels matched (or \code{NULL}).}
\item{moments}{\code{list(env_means, env_sds, trait_means_res, trait_sds_res)} used for scaling.}
\item{info}{\code{list(notes=character())} with human-readable notes.}
}
}
\description{
Column-wise z-scores \strong{environment} and \strong{resident trait} numerics, then
scales \strong{invader trait} numerics \strong{using resident moments only} (to avoid
information leakage). Invader factor/character columns are coerced to the
resident levels; unseen levels become \code{NA}. Optionally drops invader-only
columns so the resident/invader trait schemas match.
}
\details{
\strong{What gets standardised and how}
\itemize{
\item \strong{Environment (\code{env_df})}: numeric columns are z-scored (mean 0, sd 1);
non-numeric columns are kept as-is. Zero variance is guarded by setting sd=1.
\item \strong{Resident traits (\code{traits_res})}: numeric columns are z-scored; mixed
types allowed—non-numeric columns are kept.
\item \strong{Invader traits (\code{traits_inv})}: numeric columns are scaled using the
\strong{resident} trait means/SDs only (never computed from invaders). Factor/
character columns are coerced to resident levels; unseen levels become \code{NA}.
Extra invader columns are dropped (with a note).
}

\strong{Returned objects}
\itemize{
\item \code{env_df_z}: environment table with numeric columns standardised (or \code{NULL})
\item \code{traits_res_glmm}: resident traits with numeric columns standardised
\item \code{traits_inv_glmm}: invader traits, scaled \strong{like residents} + factor levels matched (or \code{NULL})
\item \code{moments}: resident/reference moments used for scaling (\verb{env_*}, \verb{trait_*})
\item \code{info$notes}: human-readable notes on coercions/drops
}

\strong{Where this is used in the workflow}
\itemize{
\item Called explicitly prior to GLMM fitting and when harmonising invaders,
and implicitly by wrappers such as \code{prepare_trait_space()} (if available).
}
}
\section{Invariants and guards}{

\itemize{
\item Column names and row names are preserved.
\item Zero-variance numeric columns use \code{sd=1} so z-scores stay defined.
\item Invader trait numerics are always scaled by \strong{resident} moments (no leakage).
\item Invader extra columns are dropped for alignment; missing required columns error.
}
}

\examples{
# Minimal reproducible example ----------------------------------------------
set.seed(1)
env_df = data.frame(elev = rnorm(5), temp = rnorm(5), zone = factor(sample(c("A","B"), 5, TRUE)))
rownames(env_df) = paste0("s", 1:5)

traits_res = data.frame(
  size = rlnorm(4), leaf = factor(c("broad","needle","broad","needle")),
  stringsAsFactors = FALSE
)
rownames(traits_res) = paste0("sp", 1:4)

traits_inv = data.frame(
  size = c(10, 1), leaf = factor(c("broad","unknown"))  # 'unknown' -> NA after coercion
)
rownames(traits_inv) = c("inv1","inv2")

std = standardise_model_inputs(env_df, traits_res, traits_inv, verbose = FALSE)
str(std, 1)
head(std$traits_res_glmm)
head(std$traits_inv_glmm)   # note: 'leaf' for 'unknown' becomes NA

# Workflow sketch ------------------------------------------------------------
# fit = prepare_inputs(long_df = longDF, ...)         # gives fit$inputs$env_df, $traits_res
# inv  = simulate_invaders(fit$inputs$traits_res, n_inv = 10)
# std  = standardise_model_inputs(fit$inputs$env_df, fit$inputs$traits_res, inv)
# std$traits_res_glmm; std$traits_inv_glmm              # pass to GLMM / trait-space steps

}
\seealso{
\code{prepare_inputs()}, \code{prepare_trait_space()}, \code{build_invader_predictors()},
\code{compute_trait_space()}, \code{compute_resident_crowding()}
}
