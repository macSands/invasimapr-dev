% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learn_sensitivities.R
\name{learn_sensitivities}
\alias{learn_sensitivities}
\title{Learn sensitivities (alpha_i, beta_i, theta_i / gamma_i) and optional site effects}
\usage{
learn_sensitivities(fit, use_site_random_slopes = TRUE, lrt = TRUE)
}
\arguments{
\item{fit}{An object produced by \code{\link[=prepare_inputs]{prepare_inputs()}} and
\code{\link[=assemble_matrices]{assemble_matrices()}}, containing resident predictor matrices
(\code{r_js_z}, \code{C_js_z}, \code{S_js_z}), trait-space structures (\code{Q_res}, \code{Q_inv}),
and the resident community layout (\code{inputs$comm_res}).}

\item{use_site_random_slopes}{Logical; if \code{TRUE}, the auxiliary model includes
site-level random slopes for abiotic and crowding terms, enabling estimation
of site-varying \eqn{\alpha_{is}} and \eqn{\Gamma_{is}} when supported by the data.
Defaults to \code{TRUE}.}

\item{lrt}{Logical; if \code{TRUE}, compute Wald or likelihood-ratio tests for key
contrasts (e.g., trait-varying versus global slopes). Defaults to \code{TRUE}.}
}
\value{
The input \code{fit} object (invisibly), with an updated
\code{fit$sensitivities} list.
}
\description{
Fits an auxiliary GLMM on resident data to estimate invader-level sensitivities
to crowding and saturation (\eqn{\alpha_i}, \eqn{\beta_i}), and abiotic conversion
slopes (\eqn{\theta_i} or \eqn{\gamma_i}). When supported by the data, optional
site-level random slopes yield site-varying adjustments
(\eqn{\alpha_{is}}, \eqn{\Gamma_{is}}).

Results are written into the \code{fit$sensitivities} slot of an
\link[=new_invasimapr_fit]{invasimapr_fit} object for downstream invasion-fitness and
establishment calculations.
}
\details{
\strong{Workflow}
\enumerate{
\item Fit an auxiliary GLMM on resident responses using
\code{\link[=fit_auxiliary_residents_glmm]{fit_auxiliary_residents_glmm()}}, optionally including site-level
random slopes for \eqn{r_z} and \eqn{C_z}.
\item Convert GLMM coefficients to sensitivities
(\eqn{\alpha_i}, \eqn{\beta_i}, \eqn{\theta_i} or \eqn{\gamma_i}) using
\code{\link[=derive_sensitivities]{derive_sensitivities()}}, returning signed and unsigned variants
plus inference summaries.
\item When supported, extract site-varying effects
(\eqn{\alpha_{is}}, \eqn{\Gamma_{is}}) via
\code{\link[=site_varying_alpha_beta_gamma]{site_varying_alpha_beta_gamma()}}.
}

The resulting components are stored in \code{fit$sensitivities}, including:
\itemize{
\item global and trait-varying sensitivities;
\item inference diagnostics and clamping summaries;
\item optional site-varying matrices and compact decomposition tables.
}
}
\examples{
\dontrun{
fit <- prepare_inputs(
  sites = site_df,
  residents = resident_df,
  invaders = invader_df,
  traits = trait_df
)

fit <- learn_sensitivities(fit, use_site_random_slopes = TRUE)
names(fit$sensitivities)
}

}
\seealso{
\code{\link[=prepare_inputs]{prepare_inputs()}},
\code{\link[=assemble_matrices]{assemble_matrices()}},
\code{\link[=fit_auxiliary_residents_glmm]{fit_auxiliary_residents_glmm()}},
\code{\link[=derive_sensitivities]{derive_sensitivities()}},
\code{\link[=site_varying_alpha_beta_gamma]{site_varying_alpha_beta_gamma()}}
}
