% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_model_formula.R
\name{build_model_formula}
\alias{build_model_formula}
\title{Flexible formula constructor for residents-only trait–environment models}
\usage{
build_model_formula(
  response = "abundance",
  env_terms = NULL,
  trait_terms = NULL,
  env_df = NULL,
  trait_df = NULL,
  include_intercept = TRUE,
  include_env_main = TRUE,
  include_trait_main = TRUE,
  include_env_trait_interactions = TRUE,
  extra_fixed = NULL,
  random_intercepts = c("site", "species"),
  random_slopes = NULL,
  backend = c("glmmTMB", "lme4"),
  verbose = FALSE
)
}
\arguments{
\item{response}{Character scalar. Name of the response on the LHS
(default \code{"abundance"}).}

\item{env_terms}{Optional character vector of environment term names to include
as fixed effects. If \code{NULL}, they can be derived from \code{env_df}.}

\item{trait_terms}{Optional character vector of trait term names to include
as fixed effects. If \code{NULL}, they can be derived from \code{trait_df}.}

\item{env_df}{Optional data frame containing environment predictors; used only
to infer \code{env_terms} when \code{env_terms} is \code{NULL}.}

\item{trait_df}{Optional data frame containing trait predictors; used only
to infer \code{trait_terms} when \code{trait_terms} is \code{NULL}.}

\item{include_intercept}{Logical. Include the fixed-effect intercept?
If \code{FALSE}, the intercept is removed via \code{0} (equivalent to \code{-1}).
Default \code{TRUE}.}

\item{include_env_main}{Logical. Include environment main effects?
Default \code{TRUE}.}

\item{include_trait_main}{Logical. Include trait main effects?
Default \code{TRUE}.}

\item{include_env_trait_interactions}{Logical. Include all pairwise
environment-by-trait interactions? Implemented as
\code{(E1 + E2 + ...):(T1 + T2 + ...)}. Default \code{TRUE}.}

\item{extra_fixed}{Optional character vector of additional fixed-effect terms
to append verbatim (e.g., \code{"poly(temp,2)"}, \code{"I(pH^2)"}).}

\item{random_intercepts}{Character vector of grouping factors for random
intercepts (e.g., \code{c("site","species")}). Use \code{NULL} to omit.
Default \code{c("site","species")}.}

\item{random_slopes}{Named list of the form
\code{list(site = c("r_z","C_z"), species = "r_z")} to add zero-correlation
slopes \code{(0 + term || group)}. Use \code{NULL} (default) for none.}

\item{backend}{Character flag used only for messaging; both \strong{lme4}
and \strong{glmmTMB} accept the same syntax here. Default \code{c("glmmTMB","lme4")}.}

\item{verbose}{Logical. If \code{TRUE}, prints the assembled formula string.}
}
\value{
An object of class \code{formula}, e.g.:
\code{abundance ~ env1 + env2 + tr1 + tr2 + (env1 + env2):(tr1 + tr2) + (1 | site) + (1 | species) + (0 + r_z || site)}
}
\description{
\code{build_model_formula()} assembles a right-hand side (RHS) for a GLMM (or LM/GLM)
from environment terms, trait terms, and (optionally) all pairwise
environment-by-trait interactions. It also appends random-effects structures,
such as \code{(1 | site) + (1 | species)} and optional zero-correlation random
slopes like \code{(0 + r_z || site)}.

You can pass the terms directly as character vectors, or let the function
derive them from \code{env_df} and/or \code{trait_df} column names.
}
\details{
Build a GLMM-ready model formula from trait and environment terms
}
\examples{
\dontrun{
# Toy data
set.seed(1)
env_df_z        = data.frame(env1 = rnorm(10), env2 = rnorm(10))
traits_res_glmm = data.frame(tr1  = rnorm(5),  tr2  = rnorm(5))

fml = build_model_formula(
  response   = "abundance",
  env_df     = env_df_z,
  trait_df   = traits_res_glmm,
  random_intercepts = c("site","species"),
  random_slopes     = list(site = c("r_z","C_z"))
)
fml

# Fit a GLMM (example; requires your long residents×sites table `dat_r`)
# library(glmmTMB)
# fit = glmmTMB::glmmTMB(fml, family = glmmTMB::tweedie(link="log"), data = dat_r)
# summary(fit)
}

}
