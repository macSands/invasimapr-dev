% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardise_by_site.R
\name{standardise_by_site}
\alias{standardise_by_site}
\title{Standardize a site-by-species matrix by site (row-wise z) with optional robust SD}
\usage{
standardise_by_site(X, robust = FALSE)
}
\arguments{
\item{X}{Numeric matrix (sites × species).}

\item{robust}{Logical. If TRUE, uses max(MAD, SD, 1e-8) per row; else classical SD with a safe 1 fallback.}
}
\value{
List with \code{z} (z-scored matrix), \code{mu} (row means), \code{sd} (row scales).
}
\description{
For each site (row), subtract the row-mean and divide by a row-scale.
The scale can be classical SD or a robust max(MAD, SD) with a small floor.
}
\examples{
\dontrun{
std = standardise_by_site(C_js, robust = TRUE)
C_js_z = std$z
}
}
