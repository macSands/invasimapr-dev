% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_resident_glmm.R
\name{prep_resident_glmm}
\alias{prep_resident_glmm}
\title{Build residents-by-sites long table and fit the residents-only GLMM}
\usage{
prep_resident_glmm(
  comm_res,
  env_df_z,
  traits_res_glmm,
  fml,
  family = glmmTMB::tweedie(link = "log"),
  seed = 123,
  fit_model = TRUE
)
}
\arguments{
\item{comm_res}{Numeric matrix (sites × residents) of observed counts or abundance.
Row names are site IDs; column names are resident species IDs.}

\item{env_df_z}{Data frame of standardized site-level environment.
Row names are site IDs and must match \code{rownames(comm_res)}.}

\item{traits_res_glmm}{Data frame of standardized resident traits used in the GLMM.
Row names are resident IDs and must match \code{colnames(comm_res)}.}

\item{fml}{A model formula for \code{glmmTMB::glmmTMB} (e.g., from \code{build_model_formula()}).}

\item{family}{A \code{glmmTMB} family object. Default is \code{glmmTMB::tweedie(link="log")}.}

\item{seed}{Integer. Random seed.}

\item{fit_model}{logical; if FALSE, return dat_r/fml without fitting.}
}
\value{
list(dat_r, fit_r, grid_res, r_js, mu_js, sites, res_ids)
}
\description{
Creates a long data set with one row per (site, resident) pair, attaches
standardized environment and resident traits, then fits a GLMM for residents.
Also returns fixed-effect predictions on the link scale as a site×resident
matrix (r_js) and the response-scale mean (mu_js).
}
