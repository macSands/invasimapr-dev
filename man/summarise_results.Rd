% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_results.R
\name{summarise_results}
\alias{summarise_results}
\title{Summarise invasiveness and invasibility (tables, maps, rankings)}
\usage{
summarise_results(
  fit,
  boundary_sf = NULL,
  boundary_params = list(inherit.aes = FALSE, fill = NA, color = "black", size = 0.3),
  traits_inv = NULL,
  ...
)
}
\arguments{
\item{fit}{An object produced by the invasimapr workflow, containing
\code{fitness} and/or \code{prob} components created by upstream steps.}

\item{boundary_sf}{Optional object of class \code{sf} providing a boundary layer
to overlay on the site map.}

\item{boundary_params}{Named list of aesthetics passed to
\code{ggplot2::geom_sf()}. Defaults to
\code{list(inherit.aes = FALSE, fill = NA, color = "black", size = 0.3)}.}

\item{traits_inv}{Optional override of the invader trait table used for
invader-ranked summaries. If \code{NULL}, traits are taken from the container.}

\item{...}{Additional arguments forwarded to
\link{summarise_invasiveness_invasibility}.}
}
\value{
The input \code{fit} object with an added or updated \code{fit$summary} list.
Invisibly returns \code{fit} to support piping.
}
\description{
Thin wrapper around \link{summarise_invasiveness_invasibility} that extracts
inputs from a \link{new_invasimapr_fit} container, forwards optional arguments,
and optionally overlays a boundary layer on the site map. This function is
intended as a reporting step after computing invasion fitness and/or
establishment probabilities.
}
\details{
\strong{Inputs used from the container}
\itemize{
\item \code{fit$fitness$lambda_is}: site-by-invader invasion fitness matrix (optional).
\item \code{fit$prob$p_is}: site-by-invader establishment probability matrix (optional).
\item \code{fit$inputs$site_df}: site metadata with coordinates \code{x} and \code{y} (optional).
\item \code{fit$inputs$comm_res}: resident community matrix for context summaries (optional).
\item invader trait tables stored in the container, used for invader rankings.
}

At least one of \code{lambda_is} or \code{p_is} must be present; otherwise an error
is thrown.

\strong{Behaviour}
\enumerate{
\item Extract invasion fitness and/or establishment probability matrices
from the container.
\item Select site metadata and an effective invader trait table when available.
\item Call \link{summarise_invasiveness_invasibility} to construct tidy
summary tables and plots (site maps, rankings, heatmaps).
\item If a boundary layer is supplied and plots are available, overlay it
on the site map using \code{geom_sf()}.
}

\strong{Output layout}
The returned container gains a \code{summary} element mirroring the structure
returned by \link{summarise_invasiveness_invasibility}, typically containing
summary tables, plots, and the arguments used.
}
\examples{
\dontrun{
fit <- prepare_inputs(
  sites = site_df,
  residents = resident_df,
  invaders = invader_df,
  traits = trait_df
)

fit <- learn_sensitivities(fit)
fit <- predict_invaders(fit, traits_inv = invader_traits)

fit <- summarise_results(fit)
fit$summary$plots$site_map
}

}
\seealso{
\link{compute_invasion_fitness},
\link{compute_establishment_probability},
\link{summarise_invasiveness_invasibility},
\link{assemble_matrices}
}
